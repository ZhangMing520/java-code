1. 异步调用
   - begin_bookTick： 发起调用，此方法是将请求数据报文直接发出去（若当前请求有积压，则先放到队列中去），此方法不会阻塞当前的调用线程
   - Ice.AsyncResult：保留了异步调用的状态信息，可以用来查询当前调用的状态，比如是否已经发送了请求报文（或者请求报文在排队等待）、调用是否完成；也保存了当前调用方法的名称，可以用于判断是哪个业务方法的异步调用。最后，为了获取异步执行结果，需要把 Result 对象传入
   - end_XXX : 获取异步调用结果，之前说过，此方法若异步调用没完成，则会阻塞